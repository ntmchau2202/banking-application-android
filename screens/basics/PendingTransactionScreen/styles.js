
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '100%',
        padding: 10,
    },

    text: {
        fontSize: 12,
        textTransform: 'uppercase',
    }
  });
